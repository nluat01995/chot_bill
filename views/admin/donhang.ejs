<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <!-- Link thư viện Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <!-- Link CSS tùy chỉnh -->
    <link href="styles.css" rel="stylesheet" />
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="#">Phương Mint Store</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/users/donhang" id="donHangCuaBan"
                >Đơn hàng của bạn</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/" id="donHangCuaBan"
                >Thêm đơn hàng mới</a
              >
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <br />
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <h2>Tìm kiếm đơn hàng theo số điện thoại</h2>
          <form action="/users/donhang" method="GET">
            <label for="phoneNumber">Nhập số điện thoại:</label>
            <input
              type="number"
              id="phoneNumber"
              name="phoneNumber"
              class="form-control"
              required
            />
            <br />
            <button type="submit" class="btn btn-primary">Tìm kiếm</button>
          </form>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th>ID</th>
                  <th>Họ và Tên</th>
                  <th>Số điện thoại</th>
                  <th>Mã đơn hàng</th>
                  <th>Tông số lượng</th>
                  <th>Trạng thái</th>
                  <th>Ngày</th>
                  <th>Hành động</th>
                </tr>
              </thead>
              <tbody>
                <% for(var i =0;i< data.length;i++){%>
                <tr>
                  <td><%= i+1 %></td>
                  <td><%= data[i].fullName %></td>
                  <td><%= data[i].phone %></td>
                  <td><%= data[i].orderData%></td>
                  <td><%= data[i].total%></td>
                  <td><%= data[i].isActive%></td>
                  <td><%= data[i].createdAt%></td>

                  <td>
                    <!-- Nút Hủy -->
                    <button
                      class="btn btn-danger"
                      onclick="cancelOrder('DH001')"
                    >
                      Hủy
                    </button>
                  </td>
                </tr>

                <%}%>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <!-- Link thư viện Bootstrap JS (để sử dụng các tính năng của Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
